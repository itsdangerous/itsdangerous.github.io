# itsdangerous.github.io

Astro로 만드는 기술 블로그입니다. 기본 테마는 어두운 `midnight`이며, 글은 Markdown 콘텐츠로 관리합니다.

## 시작하기

```bash
npm install
npm run dev
```

프로덕션 빌드는 `dist/`에 정적 파일을 생성합니다.

```bash
npm run build
```

단위 테스트는 Vitest, 브라우저 테스트는 Playwright를 사용합니다.
